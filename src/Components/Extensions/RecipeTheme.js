import {createMuiTheme} from "@material-ui/core";

const RecipeTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#055a5b'

        },
        secondary: {
            main: '#167c80'
        }
    }
});

export default RecipeTheme;